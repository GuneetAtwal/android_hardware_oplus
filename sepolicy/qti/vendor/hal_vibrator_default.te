allow hal_vibrator_default self:netlink_socket create_socket_perms_no_ioctl;

binder_use(hal_vibrator_default)

allow hal_vibrator_default aac_richtap_device:chr_file rw_file_perms;

allow hal_vibrator_default mnt_vendor_file:dir search;

get_prop(hal_vibrator_default, system_oplus_project_prop)
